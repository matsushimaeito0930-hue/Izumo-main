# HackVerse デザインガイド

HackVerse はハッカソン運営のためのリアルタイム・ダッシュボードです。デザインの目的はひとつ。
**「どのチームが、どれだけ動いているか」を数秒で判断できること。**

## トーン

白ベージュ（紙）基調のライトテーマに、**Soft UI**（ニューモーフィズムの折衷版）を重ねています。
面は柔らかく浮かせて触感を出しつつ、数値・バー・ボタン・入力欄のコントラストは落とさない、という線引きです。

フルのニューモーフィズムは採用していません。境界と押下可能性を影だけで表現するため、
「どのチームがどれだけ動いたか」を数秒で判断させるという本来の目的と衝突するためです。

以前の3Dメタバース（Plaza）は廃止し、コードも削除済みです。

## 画面構成

**画面は2つだけ**です。初参加者が「どこを見ればいいか」で迷わないことを最優先にしています。

- `/dashboard` 開発状況: 数字2枚 → チーム比較 → アクティビティとランキング。
- `/help` 質問する: 投稿フォーム → 掲示板 → メンター相談。
- `/` 入口: 1カラム。未ログインなら「GitHubでログイン」1ボタン、ログイン後は招待コード1欄だけ。
  運営とメンター向けの操作は折りたたみに隠します。

意図的に持たないもの: チーム内チャット、開発ステージ表示、自チーム専用ページ、ランキング専用ページ。
いずれも他の画面と情報が重複し、選択肢を増やすだけでした。

## カラートークン（`tokens.css`）

| 用途 | トークン | 値 |
| --- | --- | --- |
| 背景（紙） | `--color-paper` | `#faf7f1` |
| カード面 | `--color-surface` | `#fffdfa` |
| 補助面 | `--color-sand` / `--color-paper-2` | `#f7f3ea` / `#f2ece1` |
| 罫線 | `--color-line` / `--color-line-strong` | `#e6dfd2` / `#d6cdbb` |
| 文字（主） | `--color-ink` / `--color-ink-2` | `#23201c` / `#4a443c` |
| 文字（補助） | `--color-muted` | `#7a7266` |
| 主要シグナル | `--color-pulse` | `#0f8b7e` |
| 注意シグナル | `--color-hot` | `#c05575` |
| 強調シグナル | `--color-sun` | `#b5771a` |
| 成功シグナル | `--color-field` | `#2e7d5b` |

Tailwind 側では `paper` / `surface` / `sand` / `line` / `ink` / `muted` / `pulse` / `hot` / `sun` / `field` として同じ値を参照します。

## タイポグラフィ

- 見出し: システムサンセリフ、`font-bold`、`tracking-tight`
- 本文: システムサンセリフ、標準ウェイト、行間 `leading-6` 前後
- 数値: 等幅フォント（スコア・コミット数・リポジトリ名）
- 日本語の可読性のため `font-feature-settings: "palt"` を有効化

装飾的な `uppercase` + 極端な字間は使いません。

## 余白・角丸・影（Soft UI のルール）

- 余白は `tokens.css` の4ptスケール
- 外側カードは `rounded-2xl` + `border-line/70` + `shadow-card`
- 内部要素は `rounded-xl` + `shadow-soft`
- 面の色は背景 `paper` (#faf7f1) より一段明るい `surface` (#fffdfa)。この差と影で浮きを作ります

影トークンは4種類だけです。増やさないでください。

| トークン | 用途 |
| --- | --- |
| `shadow-soft` | 小さい面（リスト行、アイコンチップ、バッジ） |
| `shadow-card` | カード、パネル、選択中のタブ |
| `shadow-inset` | 溝。プログレスバーの下地、入力欄、セグメント切替の受け皿 |
| `shadow-pressed` | ボタンの `:active` のみ。静止状態には使わない |

**守る線引き:**

- 影は装飾。**状態を影だけで伝えない** — 押せるものには必ず境界線か塗りを併用する
- 数値・ラベル・バーの塗りは影を乗せず、`ink` / `pulse` / `hot` の彩度をそのまま出す
- 入力欄は `shadow-inset` で凹ませるが、`border-line` は残す。フォーカスは `border-pulse` で示す
- 面の色を背景と完全に同色にしない（純ニューモーフィズム化の入口になるため）

## モーション

控えめに。新着アクティビティは1回だけ淡くハイライトし、バーは幅をトランジションします。
`prefers-reduced-motion` ではアニメーションを無効化します。

## 共通ルール

- 装飾カードより、比較・ラベル・実データを優先する
- UIの文言は日本語を基本にする（GitHub用語やコードは原語のまま）
- Webhook の挙動、招待フロー、Supabase のスキーマは変更しない
- ダッシュボードは、相談機能よりも先にコミット数とスコア差を提示する
- 機能を足すときは「初参加者が迷う選択肢が増えないか」を先に確認する
