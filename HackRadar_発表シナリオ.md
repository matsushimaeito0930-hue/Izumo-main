# HackRadar 発表シナリオ

合計およそ3分20秒。各見出しの秒数は目安です。
画面に書いてあることは読まず、書いていないことを話す構成にしています。
同じ内容がPowerPointの発表者ノートにも入っています。

---

## 1枚目｜表紙「開発の動きを、ひと目で。」

【20秒｜つかみ】

ハッカソンの2日目の夕方、みなさんはこう思ったことはないでしょうか。
「うちのチーム、他と比べて進んでるのか、遅れてるのか、まったく分からない」。

HackRadar は、その「見えなさ」を消すためのダッシュボードです。
特別な報告は要りません。いつもどおり GitHub に push するだけで、
チームの進み具合とランキングが勝手に更新されます。

（間を置いて）今日お伝えしたいのは1つだけです。
このアプリで参加者がやることは「選ぶこと」だけになりました。

---

## 2枚目｜なぜ作ったか

【40秒｜課題と解決】

進捗を共有する方法は今まででもありました。Slackに書く、口頭で報告する。
でも開発に集中していると、報告そのものが後回しになります。
結局、最後の発表で初めて他チームの完成度を知る。これが普通でした。

わたしたちが考えたのは「報告をやめる」という方向です。
GitHubへのpushは、報告しようとしまいと、どのチームも必ずやります。
だったら、その事実そのものを進捗として扱えばいい。
push が報告になれば、報告の手間はゼロになります。

始めるまでの流れも4ステップに絞りました。
運営がイベントを作って、チームごとの招待URLを配る。
参加者はそのURLを開いてリポジトリを選ぶ。それだけです。

---

## 3枚目｜デモ動画（40秒）

【40秒｜動画に合わせて】

（再生開始）これは実際に動いている本番環境です。

最初に立場を選びます。参加者・メンター・運営で見える画面が変わります。
運営がイベントを作ると招待コードが出て、チームを登録すると
そのチーム専用の部屋番号が発行されます。

（19秒あたり・参加フォームで）ここを見てください。
参加者は招待URLを開くだけで、2つのコードが最初から入っています。
入力はゼロです。あとはリポジトリを一覧から選ぶだけ。

（32秒あたり・ダッシュボードで）そして push すると、
スコア、順位、誰が参加しているかが、この画面に自動で並びます。

---

## 4枚目｜技術スタック

【40秒｜技術】

構成は Next.js の App Router に TypeScript と Tailwind。
データは Supabase の Postgres で、Realtime を使って即座に反映しています。
GitHub とは Webhook と REST API の両方向でつながっています。

ここで補足すると、Webhook は GitHub から受け取る側、
REST API はこちらから叩きに行く側です。方向が逆の2つを使っています。

そして下の4つが、今回いちばん時間をかけたところです。
GitHub の OAuth は、ライブラリを使わず node:crypto だけで書きました。
セッションは HS256 の JWT を httpOnly cookie に入れて、
alg を none にすり替える攻撃も通らないように検証しています。

（自信を持って）中身を理解せずにライブラリを入れるのではなく、
仕組みを分かったうえで自分で組む、という方針で作りました。

---

## 5枚目｜設計で工夫した点

【30秒｜いちばん伝えたいこと】

作っている途中で気づいたことがあります。

このアプリ、最初は参加者全員が GitHub の Settings を開いて、
Webhook の URL と Secret を手で貼る必要がありました。
初参加の人向けを名乗っているのに、それは無理だと思いました。

そこで、リポジトリを選んだ瞬間にアプリが GitHub の API を叩いて、
Webhook を代わりに登録するようにしました。
この1つで、参加者の作業が「選ぶだけ」になりました。

なりすまし対策も入れています。投稿者の名前はブラウザからの申告ではなく、
サーバーが cookie から読んだ本人情報を使うので、書き換えても通りません。

---

## 6枚目｜今後の展開

【30秒｜締め】

次にやるのは、イベントごとのポイント配分設定です。
いまは push が1点、プルリクのマージが20点と固定ですが、
これを運営が画面から変えられるようにします。
配点を変えたら過去の記録も計算し直すので、途中で変えても公平に保てます。

その先は、複数のハッカソンを同時に動かせるようにすること。
コードを「イベント用」と「チーム用」の2種類に分けてあるのは、この布石です。

（間）ハッカソンのたびに立ち上げて使えるものにしていきます。
このURLで今すぐ触れます。ありがとうございました。

---

## 想定質問への答え

**Q. バックエンドは何を使っていますか？**
Next.js の Route Handler です。`app/api` 以下に19本のAPIをサーバー側に持っています。
Expressのような別サーバーは立てず、1つのプロジェクトに同居させています。

**Q. GitHub REST API は何に使っているのですか？**
外部連携です。ログインユーザーの情報取得、リポジトリ一覧の取得、
そしてWebhookの自動登録の3か所で、こちらからGitHubを呼びに行っています。
Webhookは逆にGitHubから受け取る側なので、2つは向きが違います。

**Q. スコアの配点の根拠は？**
いまは相対的な重み付けです。pushが1点、Issue解決が8点、PR作成とレビューが10点、
PRマージが20点。pushを刻むより、レビューを通してマージまで持っていくチームが上に来ます。
絶対値に意味はないので、イベントごとに変えられるようにするのが次の課題です。

**Q. 参加者のGitHubトークンはどう扱っていますか？**
httpOnly cookieの中のJWTにだけ入れて、クライアントには一切返していません。
要求しているスコープも `read:user` と `admin:repo_hook` の2つだけで、
コードを読む権限は求めていません。

**Q. 他のチームのデータを見られてしまいませんか？**
チームIDはクライアントから送られてくるので、サーバー側で必ず所属を確認しています。
画面の選択肢を書き換えて別のチームIDを送っても、APIが拒否します。